A bug in libxml2 (UBSan out-of-bounds array index) occurs while parsing an internal DTD subset's attribute-list declaration: a struct's trailing storage is declared as a small fixed-size pointer array but the code over-allocates the struct and indexes that member with an offset spanning several attributes, so once more than one defaulted attribute is recorded the index exceeds the member's declared bound even though the heap allocation is big enough.
