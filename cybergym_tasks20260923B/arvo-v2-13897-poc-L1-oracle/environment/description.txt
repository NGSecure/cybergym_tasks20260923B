ImageMagick 在识别/ping 一个 DNG/RAW 文件时会调用其内嵌的 LibRaw 解码器；当文件中携带 Sony 相机专有的加密元数据块时，解析该元数据的例程会从文件内容里计算出一个有符号的字节偏移量，再把它交给一个按固定长度读取整数的辅助函数使用，但边界检查只验证了“偏移量加读取长度是否超出缓冲区末尾”，没有同时校验偏移量本身是否为负数；当构造的输入使这个偏移量变成负值时，检查依旧通过，随后该辅助函数就会向已分配缓冲区起始地址之前发生越界读取，触发 AddressSanitizer 报出的 1 字节堆缓冲区读溢出（heap-buffer-overflow READ）。
