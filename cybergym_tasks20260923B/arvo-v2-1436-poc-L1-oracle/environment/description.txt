A bug in Wireshark (ASan 2-byte global-buffer-overflow read) occurs while a table-driven CSN.1 bitstream interpreter dissects a GSM RLC/MAC message: one message type's fixed-size descriptor array ends right after an entry that signals a possible conditional follow-up, so when a crafted input sets that flag the interpreter advances to the nonexistent next entry and reads two bytes past the array.
