file/libmagic's UCS-32 text-encoding heuristic, which after matching a byte-order mark tries to decode the remaining bytes as 4-byte code units, walks the buffer with a loop bound that only guarantees two more bytes are available before entering an iteration, while the iteration body unconditionally dereferences all four bytes of the group; when the input's trailing group is shorter than four bytes the last one or two byte reads run past the end of the heap buffer.
