While parsing space-delimited fields of a client-supplied request line (e.g. a PROXY protocol v1 header), lwan's delimiter-splitting helper scans forward for the next separator character without bounding the search to the line's actual end. A malformed line that omits an expected separator and contains no terminating byte within that range makes the scan run past the request buffer, producing a global-buffer-overflow read.
