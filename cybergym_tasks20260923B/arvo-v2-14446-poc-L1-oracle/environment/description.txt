kimageformats (MSan build): decoding an SGI/RGB raster whose per-scanline start-offset table is shorter than the declared scanline count leaves the missing table entries unset (the table is heap-allocated without zero-initialization), and the decoder later uses one of those unset entries as a scanline data offset/length while decompressing pixel rows, triggering a use-of-uninitialized-value warning.
