kimageformats (MSan build): opening an ORA (OpenRaster) archive whose embedded PNG image has a corrupted/truncated compressed image-data stream makes the PNG decoder emit a benign chunk-error warning while decompressing rows; building that warning's message pulls in bytes from an internal decompression scratch buffer that were never fully written, so formatting the warning text reads uninitialized memory.
