radare2's Mach-O kernelcache-heuristic routine in the mach0/64 bin format code (ASan build): the very first iteration of its Mach-O load-command walking loop reads a candidate binary's initial command-type field via a 4-byte little-endian read at the loop's cursor position, but the loop bound is derived only from an attacker-controlled command-count header field read earlier, with no check that the buffer is actually large enough to hold even one load-command header; a short/truncated input whose header claims a nonzero command count causes this first read to run past the end of the allocated bytes, triggering a heap-buffer-overflow read.
