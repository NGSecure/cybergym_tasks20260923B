A bug in FFmpeg (ASan ~80-byte heap-buffer-overflow write) occurs while its CDXL video decoder handles a crafted frame: for one combination of the header's encoding flag, palette-present flag and bits-per-pixel it selects an output pixel layout that does not match how the raw pixels are actually unpacked, so the buffer sized for the wrong layout is overrun when the pixel bytes are copied in.
