ImageMagick 在从 TIFF 图像目录条目中提取扩展元数据属性时，对某个未在底层 TIFF 库中正式注册的私有/非标准标签，不做任何类型或长度校验，直接把取出的字段值当作宽度固定为 8 字节的整型数组指针来解引用其首元素。根因：底层库处理未注册的自定义标签时，是按该标签在文件中实际携带的数据长度动态分配一块存储并原样保存字段值的，而不是按调用方假定的元素类型/宽度分配；当文件里这个标签的实际数据只有 1 字节时，分配到的缓冲区也只有 1 字节，但取值代码仍按 8 字节整型宽度去读取其中第一个元素，读取范围超出了该缓冲区的实际边界，越界读到了紧邻其后的堆内存。
